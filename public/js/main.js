// js/main.js
import { initializeCamera } from './usuario/camara.js';

// Esperar a que el DOM esté completamente cargado
document.addEventListener("DOMContentLoaded", () => {
    // Inicializar la cámara
    initializeCamera();// Inicializar la cámara

});